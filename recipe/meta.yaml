{% set name = "bruco" %}
{% set version = "0.1.1" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: b624f95a7c13a4e873a1e1c3e8a42bfb44bb0cfdd869093fceb10b00c75579a7

build:
  number: 0
  skip: true  # [win]
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv "

requirements:
  host:
    - pip
    - python
    - setuptools
  run:
    - gwdatafind
    - gwpy
    - matplotlib
    - numpy
    - python
    - scipy

test:
  imports:
    - bruco
  commands:
    - python -m bruco --help
    - bruco --help

about:
  home: https://git.ligo.org/gabriele-vajente/bruco
  license: GPLv3+
  license_family: GPL
  license_file: LICENSE
  summary: Brute force coherence

extra:
  recipe-maintainers:
    - duncanmmacleod
