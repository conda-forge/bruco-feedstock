{% set name = "bruco" %}
{% set version = "0.2.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 853e6e0cc7596da142911ed784910773b6ccacd47c40a80cde3043446a301de3

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  build:
    # extras for cross-compilation
    - cross-python_{{ target_platform }}  # [build_platform != target_platform]
    - python                              # [build_platform != target_platform]
  host:
    - pip
    - python
    - setuptools
  run:
    - gpstime
    - gwdatafind
    - gwpy
    - matplotlib-base
    - numpy
    - python
    - scipy

test:
  requires:
    - pip
  imports:
    - bruco
  commands:
    # check requirements
    - python -m pip check
    # check command-line interface
    - python -m bruco --help
    - bruco --help  # [unix]

about:
  home: https://git.ligo.org/gabriele-vajente/bruco
  license: GPL-3.0-or-later
  license_family: GPL
  license_file: LICENSE
  summary: Brute force coherence

extra:
  recipe-maintainers:
    - duncanmmacleod
